package com.goalmaker.app.domain.design

/** One theme's colors for the GoalMaker mark (themes.json, "logo"), as opaque ARGB integers. */
data class LogoColors(
    val tile: Int,
    val letter: Int,
    val arrow: Int,
)
