<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ shows a splash before the first frame: only the background, so the app's own launch
         moment (the themed logo drawing its arrow) is the first thing on screen. -->
    <style name="Theme.GoalMaker" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowSplashScreenBackground">@color/window_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
    </style>
</resources>
