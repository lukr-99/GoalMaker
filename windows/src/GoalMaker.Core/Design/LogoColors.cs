namespace GoalMaker.Core.Design;

/// <summary>One theme's colors for the GoalMaker mark (themes.json, "logo"), as opaque ARGB.</summary>
public sealed record LogoColors(uint Tile, uint Letter, uint Arrow);
