namespace GoalMaker.App.ViewModels;

/// <summary>The connector action waiting for the owner's yes: both stop the current link from working.</summary>
public enum ConnectorAction
{
    Rotate,
    Revoke,
}
